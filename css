/* ════════════════════════════════════════════════
   EDUXIS · style.css  v2
   Dark / Light theme · Profile dropdown · Modal
════════════════════════════════════════════════ */

/* ── Dark theme (default) ── */
[data-theme="dark"] {
  --bg:         #080810;
  --surface:    #10101e;
  --surface2:   #181828;
  --surface3:   #20203a;
  --border:     #252540;
  --border2:    #303060;
  --text:       #eeeef5;
  --text2:      #a0a0c0;
  --muted:      #60607a;
  --shadow:     rgba(0,0,0,.5);
  --card-glow:  rgba(0,0,0,.3);
}

/* ── Light theme ── */
[data-theme="light"] {
  --bg:         #f4f4f8;
  --surface:    #ffffff;
  --surface2:   #f0f0f6;
  --surface3:   #e8e8f0;
  --border:     #dddde8;
  --border2:    #c8c8dc;
  --text:       #18182c;
  --text2:      #50507a;
  --muted:      #9090b0;
  --shadow:     rgba(0,0,0,.1);
  --card-glow:  rgba(0,0,0,.06);
}

/* ── Shared color tokens (theme-agnostic) ── */
:root {
  --amber:      #f59e0b;
  --amber-dim:  rgba(245,158,11,0.12);
  --amber-glow: rgba(245,158,11,0.28);
  --teal:       #14b8a6;
  --teal-dim:   rgba(20,184,166,0.12);
  --rose:       #f43f5e;
  --rose-dim:   rgba(244,63,94,0.12);
  --purple:     #8b5cf6;
  --purple-dim: rgba(139,92,246,0.12);
  --green:      #22c55e;
  --green-dim:  rgba(34,197,94,0.12);
  --r-sm:  8px;
  --r-md:  12px;
  --r-lg:  18px;
  --r-xl:  24px;
}

/* ── Reset ── */
*, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
html { scroll-behavior: smooth; }
body {
  font-family: 'Instrument Sans', system-ui, sans-serif;
  background: var(--bg); color: var(--text);
  min-height: 100vh; overflow-x: hidden;
  -webkit-font-smoothing: antialiased;
  transition: background .3s, color .3s;
}

/* ── Pages ── */
.page { display: none; min-height: 100vh; }
.page.active { display: block; }

/* ── Decorations ── */
.noise-overlay {
  position: fixed; inset: 0; pointer-events: none; z-index: 0; opacity: .03;
  background-image: url("data:image/svg+xml,%3Csvg viewBox='0 0 200 200' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.85' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)'/%3E%3C/svg%3E");
}
[data-theme="light"] .noise-overlay { opacity: .015; }

.orb { position: fixed; border-radius: 50%; filter: blur(80px); pointer-events: none; z-index: 0; }
.orb-1 { width:600px; height:600px; top:-200px; left:-150px; background: radial-gradient(circle, rgba(139,92,246,.15), transparent 70%); }
.orb-2 { width:500px; height:500px; top:40%; right:-200px; background: radial-gradient(circle, rgba(20,184,166,.1), transparent 70%); }
.orb-3 { width:400px; height:400px; bottom:-100px; left:30%; background: radial-gradient(circle, rgba(245,158,11,.08), transparent 70%); }
[data-theme="light"] .orb { opacity: .5; }

/* ── Logo ── */
.logo {
  font-family: 'Clash Display', sans-serif; font-weight: 700; font-size: 1.5rem;
  color: var(--text); letter-spacing: -.02em; user-select: none;
}
.logo span { color: var(--amber); }

/* ── Buttons ── */
.btn {
  font-family: 'Instrument Sans', sans-serif; font-size: .9rem; font-weight: 600;
  border: none; cursor: pointer; border-radius: var(--r-sm); transition: all .18s ease;
  padding: .6rem 1.3rem;
}
.btn-primary { background: var(--amber); color: #000; box-shadow: 0 0 24px var(--amber-glow); }
.btn-primary:hover { background: #fbbf24; transform: translateY(-1px); box-shadow: 0 6px 28px var(--amber-glow); }
.btn-ghost { background: transparent; color: var(--text2); border: 1px solid var(--border2); }
.btn-ghost:hover { border-color: var(--amber); color: var(--text); }
.btn-outline { background: transparent; color: var(--text); border: 1.5px solid var(--border2); }
.btn-outline:hover { border-color: var(--amber); color: var(--amber); }
.btn-xl { padding: .85rem 2rem; font-size: 1rem; border-radius: var(--r-md); }
.btn-full { width: 100%; padding: .85rem; border-radius: var(--r-md); font-size: 1rem; }

/* ════════════════════════════════════════════════
   LANDING
════════════════════════════════════════════════ */
#landing.active { display: flex; flex-direction: column; position: relative; }
.land-nav {
  position: relative; z-index: 10;
  display: flex; justify-content: space-between; align-items: center;
  padding: 1.4rem 3rem; border-bottom: 1px solid var(--border);
  backdrop-filter: blur(12px); background: rgba(8,8,16,.6);
}
[data-theme="light"] .land-nav { background: rgba(244,244,248,.8); }
.nav-actions { display: flex; gap: .75rem; }

.hero {
  position: relative; z-index: 5; flex: 1;
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  text-align: center; padding: 5rem 2rem 3rem; gap: 1.8rem;
}
.hero-pill {
  display: inline-flex; align-items: center; gap: .5rem;
  background: var(--amber-dim); border: 1px solid rgba(245,158,11,.25);
  border-radius: 999px; padding: .35rem 1rem; font-size: .8rem; color: var(--amber); font-weight: 600;
}
.hero h1 {
  font-family: 'Clash Display', sans-serif; font-weight: 700;
  font-size: clamp(2.8rem,7vw,5.5rem); line-height: 1.04; letter-spacing: -.03em; max-width: 820px;
}
.hero h1 em { font-style: normal; color: var(--amber); }
.hero p { font-size: 1.1rem; color: var(--text2); max-width: 480px; line-height: 1.75; }
.hero-btns { display: flex; gap: 1rem; flex-wrap: wrap; justify-content: center; }
.hero-badges { display: flex; gap: .75rem; flex-wrap: wrap; justify-content: center; margin-top: .5rem; }
.hbadge { font-size: .8rem; color: var(--text2); background: var(--surface2); border: 1px solid var(--border); border-radius: 999px; padding: .3rem .9rem; }

.land-preview { position: relative; z-index: 5; display: flex; gap: 1rem; justify-content: center; flex-wrap: wrap; padding: 0 2rem 4rem; }
.preview-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-lg); padding: 1.2rem 1.4rem; min-width: 200px; max-width: 260px; animation: floatCard 5s ease-in-out infinite; }
.preview-card:nth-child(2) { animation-delay: -1.5s; }
.preview-card:nth-child(3) { animation-delay: -3s; }
.pc-label { font-size: .72rem; color: var(--muted); text-transform: uppercase; letter-spacing: .07em; margin-bottom: .6rem; }
.pc-subject { font-weight: 600; margin-bottom: .5rem; }
.pc-bar { height: 6px; background: var(--border); border-radius: 999px; overflow: hidden; margin-bottom: .3rem; }
.pc-fill { height: 100%; border-radius: 999px; background: linear-gradient(90deg, var(--amber), var(--teal)); }
.pc-pct { font-size: .8rem; color: var(--amber); font-weight: 600; text-align: right; }
.pc-rec { font-size: .88rem; line-height: 1.5; color: var(--text2); }
.pc-xp { font-family: 'Clash Display', sans-serif; font-size: 2rem; font-weight: 700; color: var(--amber); }
.pc-lvl { font-size: .8rem; color: var(--muted); margin-top: .3rem; }

/* ════════════════════════════════════════════════
   AUTH
════════════════════════════════════════════════ */
.auth-page.active {
  display: flex; align-items: center; justify-content: center;
  background: radial-gradient(ellipse at 50% 0%, rgba(139,92,246,.18), transparent 60%), var(--bg);
  min-height: 100vh; position: relative;
}
.auth-back {
  position: absolute; top: 1.5rem; left: 1.5rem; z-index: 10;
  background: var(--surface2); border: 1px solid var(--border); color: var(--text2);
  border-radius: var(--r-sm); padding: .45rem .9rem; font-size: .85rem; cursor: pointer;
  font-family: 'Instrument Sans', sans-serif; transition: all .15s;
}
.auth-back:hover { border-color: var(--amber); color: var(--text); }
.auth-box {
  position: relative; z-index: 5;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--r-xl); padding: 2.5rem; width: 100%; max-width: 420px;
  box-shadow: 0 40px 100px var(--shadow);
}
.auth-box h2 { font-family: 'Clash Display', sans-serif; font-size: 1.6rem; font-weight: 600; margin-bottom: .3rem; }
.auth-sub { color: var(--muted); font-size: .88rem; margin-bottom: 1.6rem; }
.auth-alert { padding: .7rem 1rem; border-radius: var(--r-sm); font-size: .85rem; margin-bottom: 1rem; }
.alert-error   { background: rgba(244,63,94,.1);  border: 1px solid rgba(244,63,94,.3);  color: #fda4af; }
.alert-success { background: rgba(34,197,94,.1);  border: 1px solid rgba(34,197,94,.3);  color: #86efac; }

.field { margin-bottom: 1.1rem; }
.field label { display: block; font-size: .82rem; color: var(--text2); margin-bottom: .35rem; font-weight: 500; }
.field input {
  width: 100%; padding: .72rem 1rem; background: var(--bg); border: 1.5px solid var(--border);
  border-radius: var(--r-sm); color: var(--text); font-family: 'Instrument Sans', sans-serif;
  font-size: .95rem; transition: border-color .18s;
}
.field input:focus { outline: none; border-color: var(--amber); }
.auth-switch { text-align: center; margin-top: 1.2rem; font-size: .85rem; color: var(--muted); }
.auth-switch a { color: var(--teal); cursor: pointer; }
.auth-switch a:hover { text-decoration: underline; }

/* ════════════════════════════════════════════════
   ONBOARDING
════════════════════════════════════════════════ */
#onboarding.active { display: flex; align-items: center; justify-content: center; background: var(--bg); position: relative; min-height: 100vh; }
.ob-wrap { position: relative; z-index: 5; width: 100%; max-width: 680px; padding: 2rem; }
.ob-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 3rem; flex-wrap: wrap; gap: 1rem; }
.ob-stepper { display: flex; align-items: center; }
.ob-step {
  width: 32px; height: 32px; border-radius: 50%; border: 2px solid var(--border2); color: var(--muted);
  display: flex; align-items: center; justify-content: center; font-size: .85rem; font-weight: 600; transition: all .3s;
}
.ob-step.active { border-color: var(--amber); color: var(--amber); background: var(--amber-dim); }
.ob-step.done   { border-color: var(--green); color: var(--green); background: var(--green-dim); }
.ob-step-line { width: 48px; height: 2px; background: var(--border); }
.ob-step-label { font-size: .8rem; color: var(--muted); font-weight: 500; }
.ob-panel { display: none; animation: fadeUp .35s ease; }
.ob-panel.active { display: block; }
.ob-emoji { font-size: 3rem; margin-bottom: 1rem; }
.ob-panel h2 { font-family: 'Clash Display', sans-serif; font-size: 1.8rem; font-weight: 600; margin-bottom: .5rem; }
.ob-panel > p { color: var(--text2); font-size: .95rem; margin-bottom: 2rem; }
.ob-choices { display: grid; grid-template-columns: repeat(4,1fr); gap: 1rem; margin-bottom: 2rem; }
.ob-choice {
  background: var(--surface); border: 2px solid var(--border); border-radius: var(--r-lg);
  padding: 1.5rem 1rem; text-align: center; cursor: pointer; transition: all .18s;
  display: flex; flex-direction: column; align-items: center; gap: .4rem;
}
.ob-choice:hover { border-color: var(--border2); }
.ob-choice.selected { border-color: var(--amber); background: var(--amber-dim); }
.oc-num { font-family: 'Clash Display', sans-serif; font-size: 1.8rem; font-weight: 700; }
.oc-sub { font-size: .75rem; color: var(--muted); }
.self-eval { display: flex; flex-direction: column; gap: 1rem; margin-bottom: 2rem; }
.se-row { display: flex; align-items: center; gap: 1rem; background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-md); padding: 1rem 1.2rem; }
.se-label { flex: 1; font-weight: 500; display: flex; align-items: center; gap: .5rem; }
.se-options { display: flex; gap: .5rem; }
.se-opt { padding: .35rem .8rem; border-radius: 999px; border: 1.5px solid var(--border); font-size: .8rem; cursor: pointer; transition: all .15s; color: var(--text2); font-family: 'Instrument Sans', sans-serif; }
.se-opt:hover { border-color: var(--amber); color: var(--amber); }
.se-opt.active { border-color: var(--amber); background: var(--amber-dim); color: var(--amber); font-weight: 600; }
.ob-goals { display: grid; grid-template-columns: repeat(3,1fr); gap: .75rem; margin-bottom: 2rem; }
.ob-goal { background: var(--surface); border: 2px solid var(--border); border-radius: var(--r-md); padding: .85rem 1rem; cursor: pointer; transition: all .15s; display: flex; align-items: center; gap: .6rem; font-size: .9rem; }
.ob-goal:hover { border-color: var(--border2); }
.ob-goal.selected { border-color: var(--teal); background: var(--teal-dim); }
.ob-next-btn { width: 100%; padding: 1rem; font-size: 1rem; border-radius: var(--r-md); }
.ob-next-btn:disabled { opacity: .4; cursor: not-allowed; transform: none !important; box-shadow: none !important; }

/* ════════════════════════════════════════════════
   APP LAYOUT
════════════════════════════════════════════════ */
#app.active { display: flex; min-height: 100vh; }

/* ── Sidebar ── */
.sidebar {
  width: 256px; flex-shrink: 0; background: var(--surface); border-right: 1px solid var(--border);
  display: flex; flex-direction: column; padding: 1.4rem .75rem;
  position: fixed; top: 0; left: 0; bottom: 0; z-index: 100; overflow-y: auto;
  transition: background .3s;
}
.sidebar-logo { font-size: 1.2rem; padding: 0 .5rem 1.2rem; border-bottom: 1px solid var(--border); margin-bottom: .5rem; }
.sidebar-nav { flex: 1; }
.snav-group { margin-top: 1.2rem; }
.snav-label { font-size: .67rem; color: var(--muted); text-transform: uppercase; letter-spacing: .1em; padding: 0 .6rem; margin-bottom: .3rem; }
.snav-item {
  display: flex; align-items: center; gap: .6rem; padding: .52rem .7rem;
  border-radius: var(--r-sm); color: var(--text2); cursor: pointer; font-size: .87rem;
  transition: all .15s; margin-bottom: .1rem; border: 1px solid transparent;
}
.snav-item:hover { background: var(--surface2); color: var(--text); }
.snav-item.active { background: var(--amber-dim); color: var(--amber); border-color: rgba(245,158,11,.2); font-weight: 600; }
.snav-item span { font-size: 1rem; width: 18px; text-align: center; flex-shrink: 0; }

/* ════════════════════════════════════════════════
   PROFILE BUTTON + DROPDOWN
════════════════════════════════════════════════ */
.sidebar-profile {
  border-top: 1px solid var(--border);
  padding-top: .75rem; margin-top: .5rem;
  position: relative;
}

.profile-btn {
  display: flex; align-items: center; gap: .75rem;
  padding: .7rem .8rem; border-radius: var(--r-md);
  cursor: pointer; transition: all .18s;
  border: 1px solid transparent;
  user-select: none;
}
.profile-btn:hover { background: var(--surface2); border-color: var(--border); }
.profile-btn.open  { background: var(--amber-dim); border-color: rgba(245,158,11,.25); }

.profile-avatar {
  width: 38px; height: 38px; border-radius: 50%; flex-shrink: 0;
  background: linear-gradient(135deg, var(--purple), var(--teal));
  display: flex; align-items: center; justify-content: center;
  font-weight: 700; font-size: .95rem; color: #fff;
  box-shadow: 0 0 0 2px var(--surface), 0 0 0 4px var(--border);
  transition: box-shadow .2s;
}
.profile-btn.open .profile-avatar { box-shadow: 0 0 0 2px var(--surface), 0 0 0 4px var(--amber); }

.profile-info { flex: 1; min-width: 0; }
.profile-name { font-size: .88rem; font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
.profile-sub  { font-size: .72rem; color: var(--amber); white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }

.profile-chevron {
  font-size: 1.1rem; color: var(--muted); transition: transform .25s;
  line-height: 1;
}
.profile-btn.open .profile-chevron { transform: rotate(90deg); }

/* ── Dropdown menu ── */
.profile-menu {
  display: none; position: absolute; bottom: calc(100% + 8px); left: 0; right: 0;
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--r-lg); padding: .75rem;
  box-shadow: 0 -20px 60px var(--shadow);
  animation: menuSlide .22s ease;
  z-index: 200;
}
.profile-menu.open { display: block; }

.pm-header {
  display: flex; align-items: center; gap: .75rem; padding: .5rem .25rem .75rem;
}
.pm-avatar {
  width: 42px; height: 42px; border-radius: 50%; flex-shrink: 0;
  background: linear-gradient(135deg, var(--purple), var(--teal));
  display: flex; align-items: center; justify-content: center;
  font-weight: 700; font-size: 1rem; color: #fff;
}
.pm-name  { font-weight: 600; font-size: .9rem; }
.pm-email { font-size: .75rem; color: var(--muted); }

.pm-divider { height: 1px; background: var(--border); margin: .4rem 0; }
.pm-section-title { font-size: .67rem; color: var(--muted); text-transform: uppercase; letter-spacing: .08em; padding: .3rem .35rem .1rem; }

.pm-item {
  display: flex; align-items: center; gap: .65rem;
  padding: .6rem .5rem; border-radius: var(--r-sm);
  cursor: pointer; transition: background .15s; font-size: .88rem;
}
.pm-item:hover { background: var(--surface2); }
.pm-item.pm-danger { color: var(--rose); }
.pm-item.pm-danger:hover { background: var(--rose-dim); }
.pm-item.pm-stat-row { cursor: default; gap: 0; justify-content: space-between; padding: .5rem .25rem; }
.pm-item.pm-stat-row:hover { background: none; }

.pm-icon  { font-size: 1rem; width: 22px; text-align: center; flex-shrink: 0; }
.pm-label { flex: 1; }
.pm-arrow { font-size: 1rem; color: var(--muted); margin-left: auto; }

/* Toggle switch */
.pm-toggle {
  width: 36px; height: 20px; border-radius: 999px; background: var(--border2);
  position: relative; transition: background .25s; flex-shrink: 0; cursor: pointer;
}
.pm-toggle.active { background: var(--amber); }
.toggle-thumb {
  position: absolute; top: 2px; left: 2px;
  width: 16px; height: 16px; border-radius: 50%;
  background: #fff; transition: transform .25s;
}
.pm-toggle.active .toggle-thumb { transform: translateX(16px); }

/* Mini stats in dropdown */
.pm-mini-stat {
  flex: 1; text-align: center; background: var(--surface2);
  border-radius: var(--r-sm); padding: .5rem .25rem;
}
.pm-mini-stat span { font-family: 'Clash Display', sans-serif; font-size: 1.1rem; font-weight: 700; color: var(--amber); display: block; }
.pm-mini-stat small { font-size: .65rem; color: var(--muted); text-transform: uppercase; letter-spacing: .06em; }

/* ════════════════════════════════════════════════
   SETTINGS MODAL
════════════════════════════════════════════════ */
.modal-overlay {
  display: none; position: fixed; inset: 0; z-index: 500;
  background: rgba(0,0,0,.6); backdrop-filter: blur(4px);
  align-items: center; justify-content: center;
}
.modal-overlay.open { display: flex; }
.modal-box {
  background: var(--surface); border: 1px solid var(--border);
  border-radius: var(--r-xl); padding: 2rem; width: 100%; max-width: 460px;
  box-shadow: 0 40px 100px var(--shadow); animation: fadeUp .3s ease;
}
.modal-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 1.5rem; }
.modal-title { font-family: 'Clash Display', sans-serif; font-size: 1.2rem; font-weight: 600; }
.modal-close {
  background: var(--surface2); border: 1px solid var(--border); color: var(--text2);
  width: 30px; height: 30px; border-radius: 50%; font-size: .9rem; cursor: pointer;
  display: flex; align-items: center; justify-content: center; transition: all .15s;
}
.modal-close:hover { border-color: var(--rose); color: var(--rose); }

.settings-section { margin-bottom: 1.3rem; }
.settings-label { font-size: .82rem; color: var(--text2); margin-bottom: .4rem; font-weight: 500; }
.settings-input {
  width: 100%; padding: .7rem 1rem; background: var(--bg); border: 1.5px solid var(--border);
  border-radius: var(--r-sm); color: var(--text); font-family: 'Instrument Sans', sans-serif;
  font-size: .93rem; transition: border-color .18s;
}
.settings-input:focus { outline: none; border-color: var(--amber); }

.theme-options { display: flex; gap: .75rem; }
.theme-opt {
  flex: 1; display: flex; align-items: center; gap: .5rem; justify-content: center;
  padding: .75rem; border-radius: var(--r-md); border: 2px solid var(--border);
  cursor: pointer; transition: all .18s; font-size: .9rem; font-weight: 500;
}
.theme-opt:hover { border-color: var(--border2); }
.theme-opt.selected { border-color: var(--amber); background: var(--amber-dim); }
.dark-opt  { }
.light-opt { }

.modal-actions { display: flex; gap: .75rem; justify-content: flex-end; margin-top: 1.5rem; }

/* ════════════════════════════════════════════════
   MAIN CONTENT
════════════════════════════════════════════════ */
.app-main {
  margin-left: 256px; flex: 1; padding: 1.8rem 2rem;
  max-width: calc(100vw - 256px); min-height: 100vh;
  transition: background .3s;
}
.app-topbar { display: flex; justify-content: space-between; align-items: flex-start; margin-bottom: 2rem; }
.app-title  { font-family: 'Clash Display', sans-serif; font-size: 1.7rem; font-weight: 600; letter-spacing: -.02em; }
.app-crumb  { font-size: .8rem; color: var(--muted); margin-top: .2rem; }

.topbar-chips { display: flex; gap: .6rem; align-items: center; }
.chip { padding: .4rem .9rem; border-radius: 999px; font-size: .82rem; font-weight: 600; }
.chip-xp   { background: var(--amber-dim); border: 1px solid rgba(245,158,11,.25); color: var(--amber); }
.chip-lvl  { background: var(--teal-dim);  border: 1px solid rgba(20,184,166,.25);  color: var(--teal); }
.chip-class{ background: var(--purple-dim);border: 1px solid rgba(139,92,246,.25); color: var(--purple); }

.panel { display: none; animation: fadeUp .3s ease; }
.panel.active { display: block; }

.card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-lg); padding: 1.5rem; margin-bottom: 1.5rem; transition: background .3s; }
.card-title { font-family: 'Clash Display', sans-serif; font-size: 1rem; font-weight: 600; margin-bottom: 1.2rem; }

/* ════════════════════════════════════════════════
   DASHBOARD
════════════════════════════════════════════════ */
.dash-stats-grid { display: grid; grid-template-columns: repeat(4,1fr); gap: 1rem; margin-bottom: 1.5rem; }
.stat-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-lg); padding: 1.2rem; position: relative; overflow: hidden; transition: background .3s; }
.stat-card::before { content:''; position: absolute; top: 0; left: 0; right: 0; height: 2px; }
.stat-card.amber::before { background: var(--amber); }
.stat-card.teal::before  { background: var(--teal); }
.stat-card.purple::before{ background: var(--purple); }
.stat-card.rose::before  { background: var(--rose); }
.sc-icon  { font-size: 1.4rem; margin-bottom: .6rem; }
.sc-val   { font-family: 'Clash Display', sans-serif; font-size: 1.9rem; font-weight: 700; }
.sc-label { font-size: .75rem; color: var(--muted); margin-top: .25rem; text-transform: uppercase; letter-spacing: .06em; }

.dash-mid { display: grid; grid-template-columns: 1fr 1fr; gap: 1.5rem; margin-bottom: 1.5rem; }
#spider-chart { display: block; margin: 0 auto; }

.rec-item { display: flex; gap: .8rem; padding: .85rem; background: var(--surface2); border-radius: var(--r-md); margin-bottom: .75rem; border: 1px solid var(--border); align-items: flex-start; }
.rec-icon { font-size: 1.2rem; flex-shrink: 0; }
.rec-text { font-size: .88rem; line-height: 1.5; color: var(--text2); }
.rec-text strong { color: var(--text); }
.rec-btn { display: inline-block; margin-top: .5rem; font-size: .78rem; color: var(--amber); font-weight: 600; cursor: pointer; text-decoration: underline; }

.subj-prog-grid { display: grid; grid-template-columns: repeat(2,1fr); gap: 1rem; }
.spg-row { display: flex; justify-content: space-between; align-items: baseline; margin-bottom: .35rem; font-size: .85rem; }
.spg-pct { font-size: .78rem; color: var(--muted); }
.prog-bar { height: 7px; background: var(--surface3); border-radius: 999px; overflow: hidden; }
.prog-fill { height: 100%; border-radius: 999px; background: linear-gradient(90deg, var(--amber), var(--teal)); transition: width 1.2s ease; }

/* ════════════════════════════════════════════════
   CATALOG
════════════════════════════════════════════════ */
.pole-block { margin-bottom: 2rem; }
.pole-heading { font-family: 'Clash Display', sans-serif; font-size: 1.1rem; font-weight: 600; margin-bottom: 1rem; padding-bottom: .6rem; border-bottom: 1px solid var(--border); }
.pole-heading.sci  { color: var(--teal); }
.pole-heading.lit  { color: var(--amber); }
.pole-heading.lang { color: var(--purple); }
.catalog-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(180px,1fr)); gap: 1rem; }
.cat-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: var(--r-lg); padding: 1.2rem; cursor: pointer; transition: all .18s; display: flex; flex-direction: column; gap: .4rem; }
.cat-card:hover { border-color: var(--amber); transform: translateY(-2px); box-shadow: 0 8px 30px var(--card-glow); }
.cat-icon { font-size: 1.8rem; }
.cat-name { font-family: 'Clash Display', sans-serif; font-size: .95rem; font-weight: 600; }
.cat-chapters { font-size: .75rem; color: var(--muted); }
.cat-pct { font-size: .78rem; color: var(--amber); font-weight: 600; margin-top: .3rem; }
.cat-bar .prog-bar { height: 4px; margin-top: .4rem; }

/* ════════════════════════════════════════════════
   SUBJECT
════════════════════════════════════════════════ */
.subject-hero { display: flex; align-items: center; gap: 1.5rem; background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-xl); padding: 1.8rem; margin-bottom: 1.5rem; }
.sh-icon { font-size: 3rem; }
.sh-info h2 { font-family: 'Clash Display', sans-serif; font-size: 1.6rem; font-weight: 600; }
.sh-info p { color: var(--muted); font-size: .9rem; margin-top: .25rem; }
.sh-stats { display: flex; gap: 1.5rem; margin-top: .8rem; }
.sh-stat { font-size: .82rem; color: var(--text2); }
.sh-stat strong { color: var(--amber); font-size: 1rem; display: block; }

.chapter-cards { display: flex; flex-direction: column; gap: .85rem; }
.chap-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: var(--r-lg); padding: 1.2rem 1.5rem; cursor: pointer; transition: all .18s; display: flex; align-items: center; gap: 1rem; }
.chap-card:hover { border-color: var(--amber); background: var(--surface2); }
.chap-num { width: 38px; height: 38px; border-radius: var(--r-sm); background: var(--surface3); flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-family: 'Clash Display', sans-serif; font-weight: 700; }
.chap-num.done { background: var(--green-dim); color: var(--green); }
.chap-info { flex: 1; }
.chap-name { font-weight: 600; margin-bottom: .2rem; }
.chap-meta { font-size: .78rem; color: var(--muted); }
.chap-right { display: flex; align-items: center; gap: .75rem; flex-shrink: 0; }
.diff-tag { font-size: .72rem; font-weight: 600; padding: .22rem .6rem; border-radius: 4px; }
.diff-easy   { background: var(--green-dim);  color: var(--green); }
.diff-medium { background: var(--amber-dim);  color: var(--amber); }
.diff-hard   { background: var(--rose-dim);   color: var(--rose); }
.xp-tag { font-size: .75rem; color: var(--amber); font-weight: 600; }

/* ════════════════════════════════════════════════
   LESSON
════════════════════════════════════════════════ */
.back-pill {
  display: inline-flex; align-items: center; background: var(--surface2); border: 1px solid var(--border);
  color: var(--text2); border-radius: 999px; padding: .45rem 1rem; font-size: .85rem; cursor: pointer;
  font-family: 'Instrument Sans', sans-serif; transition: all .15s; margin-bottom: 1.5rem;
}
.back-pill:hover { border-color: var(--amber); color: var(--amber); }
.lesson-layout { display: grid; grid-template-columns: 1fr 340px; gap: 1.5rem; }
.lesson-card { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-xl); padding: 2rem; }
.lesson-tag   { font-size: .75rem; color: var(--teal); text-transform: uppercase; letter-spacing: .08em; margin-bottom: .5rem; }
.lesson-title { font-family: 'Clash Display', sans-serif; font-size: 1.6rem; font-weight: 600; margin-bottom: 1.5rem; }
.lesson-body  { line-height: 1.8; color: var(--text2); font-size: .95rem; }
.lesson-body h3 { font-family: 'Clash Display', sans-serif; color: var(--text); font-size: 1.05rem; margin: 1.2rem 0 .5rem; }
.lesson-body .formula { background: var(--surface3); border-left: 3px solid var(--amber); border-radius: 0 var(--r-sm) var(--r-sm) 0; padding: 1rem 1.2rem; margin: 1rem 0; font-family: monospace; font-size: 1.05rem; color: var(--amber); }
.lesson-body .tip { background: var(--teal-dim); border: 1px solid rgba(20,184,166,.2); border-radius: var(--r-md); padding: .9rem 1rem; margin: 1rem 0; font-size: .9rem; }
.lesson-body .tip::before { content: '💡 '; }
.lesson-cta-card { background: var(--surface); border: 1.5px solid var(--border); border-radius: var(--r-xl); padding: 1.5rem; position: sticky; top: 1.5rem; }
.lcc-title { font-family: 'Clash Display', sans-serif; font-size: 1.05rem; margin-bottom: .5rem; }
.lcc-desc  { font-size: .85rem; color: var(--muted); margin-bottom: 1.2rem; line-height: 1.5; }
.lcc-stats { display: flex; gap: 1rem; margin-bottom: 1.2rem; }
.lcc-stat  { text-align: center; flex: 1; background: var(--surface2); border-radius: var(--r-md); padding: .7rem; }
.lcc-stat .v { font-family: 'Clash Display', sans-serif; font-size: 1.3rem; color: var(--amber); }
.lcc-stat .l { font-size: .7rem; color: var(--muted); margin-top: .1rem; }

/* ════════════════════════════════════════════════
   EXERCISE
════════════════════════════════════════════════ */
.ex-wrap { max-width: 680px; }
.ex-track { display: flex; gap: .4rem; margin-bottom: 1.5rem; flex-wrap: wrap; }
.ex-dot { width: 10px; height: 10px; border-radius: 50%; background: var(--surface3); transition: all .3s; }
.ex-dot.done    { background: var(--green); }
.ex-dot.current { background: var(--amber); box-shadow: 0 0 8px var(--amber); }
.ex-dot.wrong   { background: var(--rose); }
.ex-box { background: var(--surface); border: 1px solid var(--border); border-radius: var(--r-xl); padding: 2rem; }
.ex-type { font-size: .75rem; color: var(--teal); text-transform: uppercase; letter-spacing: .08em; margin-bottom: .4rem; }
.ex-num  { font-size: .8rem; color: var(--muted); margin-bottom: 1rem; }
.ex-question { font-size: 1.12rem; font-weight: 500; line-height: 1.6; margin-bottom: 1.8rem; }

.mcq-list { display: flex; flex-direction: column; gap: .65rem; }
.mcq-opt { padding: .85rem 1.1rem; border-radius: var(--r-md); border: 1.5px solid var(--border); cursor: pointer; font-size: .93rem; transition: all .15s; font-family: 'Instrument Sans', sans-serif; }
.mcq-opt:hover    { border-color: var(--amber); background: var(--amber-dim); }
.mcq-opt.selected { border-color: var(--amber); background: var(--amber-dim); }
.mcq-opt.correct  { border-color: var(--green); background: var(--green-dim); }
.mcq-opt.wrong    { border-color: var(--rose);  background: var(--rose-dim); }

.calc-row { display: flex; align-items: center; gap: 1rem; margin-bottom: 1.5rem; }
.calc-row input { background: var(--surface2); border: 1.5px solid var(--border2); border-radius: var(--r-md); padding: .65rem 1.1rem; color: var(--text); font-size: 1.3rem; font-weight: 700; width: 130px; text-align: center; font-family: 'Clash Display', sans-serif; }
.calc-row input:focus { outline: none; border-color: var(--amber); }
.calc-unit { font-size: 1rem; color: var(--muted); }

.blank-sentence { display: flex; flex-wrap: wrap; align-items: center; gap: .4rem; font-size: 1.05rem; line-height: 2.2; margin-bottom: 1.2rem; }
.blank-slot { display: inline-block; min-width: 90px; height: 34px; border-bottom: 2px solid var(--teal); background: var(--teal-dim); border-radius: 4px 4px 0 0; cursor: pointer; line-height: 34px; text-align: center; color: var(--teal); font-weight: 600; padding: 0 .5rem; transition: all .15s; }
.blank-slot.filled  { background: rgba(20,184,166,.15); }
.blank-slot.correct { border-color: var(--green); color: var(--green); }
.blank-slot.wrong   { border-color: var(--rose);  color: var(--rose); }
.word-bank { display: flex; flex-wrap: wrap; gap: .5rem; background: var(--surface2); border-radius: var(--r-md); padding: .9rem; margin-bottom: 1.2rem; }
.word-chip { padding: .35rem .85rem; border-radius: 999px; background: var(--surface); border: 1px solid var(--border); font-size: .88rem; cursor: pointer; transition: all .15s; font-family: 'Instrument Sans', sans-serif; }
.word-chip:hover { border-color: var(--teal); }
.word-chip.selected { border-color: var(--teal); background: var(--teal-dim); color: var(--teal); }
.word-chip.used { opacity: .3; pointer-events: none; }

.match-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; margin-bottom: 1.2rem; }
.match-col  { display: flex; flex-direction: column; gap: .5rem; }
.match-item { padding: .65rem 1rem; border-radius: var(--r-md); border: 1.5px solid var(--border); cursor: pointer; font-size: .88rem; text-align: center; transition: all .15s; font-family: 'Instrument Sans', sans-serif; }
.match-item:hover       { border-color: var(--amber); }
.match-item.sel-left    { border-color: var(--amber); background: var(--amber-dim); }
.match-item.matched     { border-color: var(--green); background: var(--green-dim); color: var(--green); }
.match-item.wrong-match { border-color: var(--rose);  background: var(--rose-dim); }

.ex-feedback { display: none; margin-top: 1.2rem; padding: 1rem 1.2rem; border-radius: var(--r-md); align-items: flex-start; gap: .8rem; }
.ex-feedback.show { display: flex; }
.ex-feedback.ok  { background: var(--green-dim); border: 1px solid rgba(34,197,94,.25); }
.ex-feedback.bad { background: var(--rose-dim);  border: 1px solid rgba(244,63,94,.25); }
.fb-icon  { font-size: 1.2rem; }
.fb-title { font-weight: 600; margin-bottom: .15rem; }
.fb-desc  { font-size: .83rem; color: var(--text2); }
.ex-actions { display: flex; justify-content: flex-end; margin-top: 1.5rem; gap: .75rem; }
.correction-block { margin-top: 1rem; padding: 1rem; background: var(--surface2); border-radius: var(--r-md); border: 1px solid var(--border); font-size: .87rem; line-height: 1.6; color: var(--text2); }
.correction-block strong { color: var(--amber); }

/* ════════════════════════════════════════════════
   LEADERBOARD
════════════════════════════════════════════════ */
.lb-row { display: flex; align-items: center; gap: 1rem; padding: .85rem 1rem; border-radius: var(--r-md); border: 1px solid var(--border); background: var(--surface2); margin-bottom: .5rem; }
.lb-row.me { border-color: var(--amber); background: var(--amber-dim); }
.lb-rank { font-family: 'Clash Display', sans-serif; font-weight: 700; font-size: 1rem; width: 28px; text-align: center; }
.lb-rank.g { color: #fbbf24; }
.lb-rank.s { color: #d1d5db; }
.lb-rank.b { color: #cd7f32; }
.lb-av { width: 30px; height: 30px; border-radius: 50%; flex-shrink: 0; display: flex; align-items: center; justify-content: center; font-size: .78rem; font-weight: 700; background: linear-gradient(135deg, var(--purple), var(--teal)); color: #fff; }
.lb-info { flex: 1; }
.lb-name { font-weight: 600; font-size: .9rem; }
.lb-sub  { font-size: .75rem; color: var(--muted); }
.lb-xp   { font-family: 'Clash Display', sans-serif; font-weight: 700; color: var(--amber); }

/* ── XP Pop ── */
.xp-pop { position: fixed; z-index: 999; pointer-events: none; font-family: 'Clash Display', sans-serif; font-weight: 700; color: var(--amber); font-size: 1.5rem; animation: xpFly 1.6s ease-out forwards; }

/* ════════════════════════════════════════════════
   KEYFRAMES
════════════════════════════════════════════════ */
@keyframes fadeUp    { from { opacity:0; transform:translateY(12px); } to { opacity:1; transform:translateY(0); } }
@keyframes xpFly     { 0%{opacity:1;transform:translateY(0) scale(1)} 60%{opacity:1;transform:translateY(-55px) scale(1.25)} 100%{opacity:0;transform:translateY(-110px) scale(.8)} }
@keyframes floatCard { 0%,100%{transform:translateY(0)} 50%{transform:translateY(-10px)} }
@keyframes menuSlide { from{opacity:0;transform:translateY(8px)} to{opacity:1;transform:translateY(0)} }

/* ════════════════════════════════════════════════
   RESPONSIVE
════════════════════════════════════════════════ */
@media (max-width: 1100px) {
  .dash-stats-grid { grid-template-columns: repeat(2,1fr); }
  .dash-mid        { grid-template-columns: 1fr; }
  .lesson-layout   { grid-template-columns: 1fr; }
}
@media (max-width: 768px) {
  .sidebar { display: none; }
  .app-main { margin-left: 0; max-width: 100vw; }
  .subj-prog-grid { grid-template-columns: 1fr; }
  .ob-choices { grid-template-columns: repeat(2,1fr); }
  .ob-goals   { grid-template-columns: repeat(2,1fr); }
  .catalog-grid { grid-template-columns: repeat(2,1fr); }
}

::-webkit-scrollbar { width: 5px; }
::-webkit-scrollbar-track { background: var(--bg); }
::-webkit-scrollbar-thumb { background: var(--border2); border-radius: 3px; }
